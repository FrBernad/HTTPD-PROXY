CONNECTIONS=connections
PARSERS=parsers/*
STATE_MACHINE=state_machine
STATES=states/*
UTILS=utils/*
MANAGEMENT = management

SOURCE_DIRS=$(CONNECTIONS) $(PARSERS) $(STATE_MACHINE) $(STATES) $(UTILS) $(MANAGEMENT)