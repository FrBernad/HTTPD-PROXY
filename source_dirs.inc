CONNECTIONS=connections_manager
PARSERS=parsers/*
STATE_MACHINE=state_machine
STATES=states/*
UTILS=utils/*
METRICS=metrics
MANAGEMENT=management
LOGGER=logger

SOURCE_DIRS=$(CONNECTIONS) $(PARSERS) $(STATE_MACHINE) $(STATES) $(UTILS) $(METRICS) $(MANAGEMENT) $(LOGGER)

MANAGEMENT_CLIENT_DIRS=management_client/*